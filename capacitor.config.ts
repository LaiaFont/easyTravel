import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Multiplataforma2',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
